const { default: Form } = require("../../../../../../../../../../../../Form")

module.exports = {
    loginReq: (form) => {
        console.log(form)
    },

    regReq: (form) => {
        console.log(form)
    }
}